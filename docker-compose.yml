version: '3.8'
services:
  mongo:
    image: mongo:6
    ports: ["27017:27017"]
    volumes: [mongo-data:/data/db]
  
  wrl:
    build: ./backend
    ports: ["3000:3000"]
    depends_on: [mongo]
    volumes: [./frontend:/app/frontend]

volumes:
  mongo-data:
